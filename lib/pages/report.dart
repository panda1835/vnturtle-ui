import 'package:flutter/material.dart';

class BaoCaoViPhamPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'VNTURTLE', 
        ),
      ),
      body: Center(
        child: Text('Báo cáo vi phạm page'),
      ),
    );
  }
}