import 'package:flutter/material.dart';

class ThongTinLoaiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thông tin loài'),
      ),
      body: Center(
        child: Text('Thông tin loài page'),
      ),
    );
  }
}